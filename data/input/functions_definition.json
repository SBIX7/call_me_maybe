[
  {
    "name": "fn_system_ping",
    "description": "Ping the system to check if it is alive. Requires absolutely no parameters.",
    "parameters": {},
    "returns": {
      "type": "boolean"
    }
  },
  {
    "name": "fn_calculate_trajectory",
    "description": "Calculate trajectory given initial velocity and angle. Can handle negative values and floats.",
    "parameters": {
      "velocity": {
        "type": "number"
      },
      "angle": {
        "type": "number"
      }
    },
    "returns": {
      "type": "number"
    }
  },
  {
    "name": "fn_user_profile",
    "description": "Update user profile with age, bio, subscription status, and role.",
    "parameters": {
      "age": {
        "type": "number"
      },
      "bio": {
        "type": "string"
      },
      "is_premium": {
        "type": "boolean"
      },
      "role": {
        "type": "string",
        "enum": [
          "admin",
          "user",
          "guest"
        ]
      }
    },
    "returns": {
      "type": "string"
    }
  },
  {
    "name": "fn_regex_extractor",
    "description": "Extract data from a text using a regex pattern.",
    "parameters": {
      "text": {
        "type": "string"
      },
      "pattern": {
        "type": "string"
      }
    },
    "returns": {
      "type": "string"
    }
  },
  {
    "name": "fn_smart_home_control",
    "description": "Control a smart home device state.",
    "parameters": {
      "device": {
        "type": "string",
        "enum": [
          "lights",
          "thermostat",
          "blinds",
          "lock"
        ]
      },
      "state": {
        "type": "boolean"
      }
    },
    "returns": {
      "type": "string"
    }
  }
]